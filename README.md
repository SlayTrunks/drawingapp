# in package.json in http-backend and ws-backend
in dependencies and repo name "*" for npm and "workspace:*" for pnpm package manager
pnpm install everytime we change something manually by hand in package.json



